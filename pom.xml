<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.igapyon.miku</groupId>
  <artifactId>miku-indexgen-skills</artifactId>
  <version>1.6.0</version>
  <packaging>pom</packaging>

  <name>miku-indexgen-skills</name>
  <description>Agent Skills package for miku-indexgen.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.antrun.plugin.version>3.1.0</maven.antrun.plugin.version>
    <skill.name>igapyon-miku-indexgen</skill.name>
    <skill.dir>${project.basedir}/skills/${skill.name}</skill.dir>
    <indexgen.java.version>1.6.0</indexgen.java.version>
    <indexgen.jar>${skill.dir}/runtime/miku-indexgen-${indexgen.java.version}.jar</indexgen.jar>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>${maven.antrun.plugin.version}</version>
        <executions>
          <execution>
            <id>generate-skill-index</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target name="generate-skill-index">
                <java jar="${indexgen.jar}" fork="true" failonerror="true">
                  <arg value="--input-directory" />
                  <arg value="${skill.dir}" />
                  <arg value="--title" />
                  <arg value="igapyon-miku-indexgen Skill Index" />
                  <arg value="--include-ext" />
                  <arg value="md,mjs,json" />
                </java>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
