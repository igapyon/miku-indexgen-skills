# TODO

- Pin the next `miku-indexgen` and `miku-indexgen-java` runtime artifacts when new GitHub Releases are adopted.
- Add product-specific regression fixtures if upstream `miku-indexgen` exposes more structured diagnostics or API contracts.
